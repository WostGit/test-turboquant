name: TurboQuant Real Prompt Test - TinyLlama (Correct Indentation)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel psutil
          pip install mlx mlx-lm
          git clone https://github.com/rachittshah/mlx-turboquant.git
          cd mlx-turboquant && pip install -e .

      - name: Real Prompt Test with TinyLlama
        env:
          HF_TOKEN: "dummy_token_to_suppress_warning"
        run: |
          python - << 'EOF'
          import mlx.core as mx
          from mlx_lm import load, generate
          from mlx_lm.sample_utils import make_sampler
          from mlx_turboquant.cache import TurboQuantKVCache
          import time
          import psutil
          import gc

          def get_mem():
              gc.collect()
              mx.clear_cache()
              return psutil.Process().memory_info().rss / (1024**2)

          # FIXED: Swapped to TinyLlama-1.1B (Fast download, native 64-dim heads)
          print("Loading TinyLlama-1.1B-Chat-v1.0-4bit...")
          model, tokenizer = load("mlx-community/TinyLlama-1.1B-Chat-v1.0-4bit", lazy=True)
          print(f"Loaded {len(model.layers)} layers\n")

          # Using standard TinyLlama chat template format
          prompt = "<|user|>\nWe need to add long-context support using KV cache compression. Please implement a 3-bit TurboQuant style compression layer.<|assistant|>\n"

          current_sampler = make_sampler(temp=0.6)

          print("=== BASELINE ===")
          start_mem = get_mem()
          start = time.time()
          
          response_baseline = generate(
              model, 
              tokenizer, 
              prompt=prompt, 
              max_tokens=150, 
              verbose=False, 
              sampler=current_sampler
          )
          baseline_time = time.time() - start
          baseline_mem = get_mem() - start_mem

          print(f"Time: {baseline_time:.2f}s | Mem Δ: {baseline_mem:.1f} MB")
          print("Response preview:")
          print(response_baseline[:500])
          print("-" * 80)

          print("\n=== TURBOQUANT 3-BIT ===")
          start_mem = get_mem()
          start = time.time()

          try:
              # Dynamically query dimensions from model configuration
              head_dim = model.args.hidden_size // model.args.num_attention_heads
              print(f"Initializing TurboQuant cache with head_dim={head_dim}...")
              
              cache = [TurboQuantKVCache(bits=3, head_dim=head_dim) for _ in range(len(model.layers))]

              # Warm-up cache
              tokens = mx.array([tokenizer.encode(prompt)])
              _ = model(tokens, cache=cache)

              # Generate real response passing the custom cache
              response_tq = generate(
                  model, 
                  tokenizer, 
                  prompt=prompt, 
                  prompt_cache=cache, 
                  max_tokens=150, 
                  verbose=False, 
                  sampler=current_sampler
              )
              tq_time = time.time() - start
              tq_mem = get_mem() - start_mem

              print(f"Time: {tq_time:.2f}s | Mem Δ: {tq_mem:.1f} MB")
              print("Response preview:")
              print(response_tq[:500])
          except Exception as e:
              print(f"TurboQuant failed: {type(e).__name__} - {str(e)[:300]}")

          print("\n=== SUMMARY ===")
          print("If this fails or repeats characters, 3-bit is simply too low precision for sub-7B models on this fork.")
          EOF
