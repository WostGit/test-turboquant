name: Qwen 7B Fast Context Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  long-context-test:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Base Dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel psutil
          # ADDED hf_transfer for blazing fast downloads
          pip install mlx mlx-lm numpy hf_transfer

      - name: Test Long Context Cache
        env:
          # This forces Hugging Face to use the Rust-based fast downloader
          HF_HUB_ENABLE_HF_TRANSFER: "1"
        run: |
          python - << 'EOF'
          import os
          import mlx.core as mx
          from mlx_lm import load, generate
          
          model_name = "mlx-community/Qwen2.5-7B-Instruct-4bit"
          print("⏳ Loading model with Rust hf_transfer...")
          
          model, tokenizer = load(model_name, lazy=True)
          
          # 1500 words of filler text to force the KV cache to work
          filler_text = "The quick brown fox jumps over the lazy dog. " * 300
          secret_fact = "The secret password is 'BANANA-CAKE'."
          
          prompt = f"<|im_start|>user\nHere is a massive document:\n{filler_text}\n{secret_fact}\n{filler_text}\nBased on the text, what is the secret password?<|im_end|>\n<|im_start|>assistant\n"
          
          print(f"📊 Prompt length is roughly {len(prompt.split())} words.")
          
          try:
              print("▶ Running generation on 8-bit KV cache...")
              resp = generate(
                  model, 
                  tokenizer, 
                  prompt=prompt, 
                  max_tokens=20, 
                  verbose=False,
                  kv_bits=8 # Quantized 8-bit cache
              )
              print(f"Result: {resp.strip()}")
              
          except Exception as e:
              print(f"Execution Failed: {e}")
          EOF
