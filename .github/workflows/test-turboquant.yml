name: TurboQuant Final Fixed Execution

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  force-test:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Base Dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel psutil
          pip install mlx mlx-lm numpy

      # =====================================================================
      # REPO 1: Rachitt Shah (Fixed to max-supported 4-bit)
      # =====================================================================
      - name: Test Repo 1 - Rachitt Shah (Fixed 4-bit)
        continue-on-error: true
        run: |
          git clone https://github.com/rachittshah/mlx-turboquant.git
          cd mlx-turboquant
          pip install -e .
          
          python - << 'EOF'
          import mlx.core as mx
          from mlx_lm import load, generate
          from mlx_turboquant.cache import TurboQuantKVCache
          
          model_name = "mlx-community/Qwen2.5-0.5B-Instruct-4bit"
          model, tokenizer = load(model_name, lazy=True)
          prompt = "<|im_start|>user\nWrite a 5-word sentence.<|im_end|>\n<|im_start|>assistant\n"
          
          try:
              head_dim = model.args.hidden_size // model.args.num_attention_heads
              
              # FIXED: Reverted to 4-bits so the Lloyds-Max codebook doesn't throw a missing file error
              cache = [TurboQuantKVCache(bits=4, head_dim=head_dim) for _ in range(len(model.layers))]
              
              print("▶ Running Rachitt Shah (4-bit)...")
              resp = generate(model, tokenizer, prompt=prompt, prompt_cache=cache, max_tokens=15, verbose=False)
              print(f"Result: {resp.strip()}")
          except Exception as e:
              print(f"Execution Failed: {e}")
          EOF

      # =====================================================================
      # REPO 2: Anton Rozanov (Verified Path Hack)
      # =====================================================================
      - name: Test Repo 2 - Anton Rozanov (Path Hack)
        continue-on-error: true
        run: |
          git clone https://github.com/arozanov/turboquant-mlx.git
          
          python - << 'EOF'
          import sys
          import os
          
          repo_path = os.path.abspath("turboquant-mlx")
          sys.path.append(repo_path)
          
          try:
              import mlx.core as mx
              from mlx_lm import load, generate
              from turboquant_mlx import make_adaptive_cache, apply_patch
              
              print("✅ Successfully bypassed broken installer using sys.path hack!")
              
              model_name = "mlx-community/Qwen2.5-0.5B-Instruct-4bit"
              model, tokenizer = load(model_name, lazy=True)
              prompt = "<|im_start|>user\nWrite a 5-word sentence.<|im_end|>\n<|im_start|>assistant\n"
              
              apply_patch()
              # Rozanov's repo defaults to 3-bit PolarQuant
              cache = make_adaptive_cache(len(model.layers), bits=3, fp16_layers=1)
              
              print("▶ Running Anton Rozanov Custom Metal Cache...")
              resp = generate(model, tokenizer, prompt=prompt, prompt_cache=cache, max_tokens=15, verbose=False)
              print(f"Result: {resp.strip()}")
              
          except Exception as e:
              print(f"Rozanov Execution Failed: {e}")
          EOF
