name: Qwen 7B KV Cache Stress Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  stress-test:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Base Dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel psutil
          pip install mlx mlx-lm numpy

      - name: Run Limit Tests
        run: |
          python - << 'EOF'
          import mlx.core as mx
          from mlx_lm import load, generate
          
          model_name = "mlx-community/Qwen2.5-7B-Instruct-4bit"
          print("⏳ Loading model...")
          model, tokenizer = load(model_name, lazy=True)
          
          prompt = "<|im_start|>user\nWrite a 5-word sentence.<|im_end|>\n<|im_start|>assistant\n"
          
          def test_limit(bit_rate, start_step):
              print(f"\n▶ Testing kv_bits={bit_rate} | quantized_kv_start={start_step}")
              try:
                  resp = generate(
                      model, 
                      tokenizer, 
                      prompt=prompt, 
                      max_tokens=15, 
                      verbose=False,
                      kv_bits=bit_rate,
                      quantized_kv_start=start_step
                  )
                  print(f"Result: {resp.strip()}")
              except Exception as e:
                  print(f"Failed at {bit_rate}-bits: {e}")

          # Test 1: Brutal 4-bit from Step 0 (No safety ramp-up)
          test_limit(bit_rate=4, start_step=0)
          
          # Test 2: Pushing to 3-bit
          test_limit(bit_rate=3, start_step=0)
          
          # Test 3: The edge of the cliff - 2-bit
          test_limit(bit_rate=2, start_step=0)

          EOF
